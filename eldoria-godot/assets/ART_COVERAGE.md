# Art Coverage Audit — auto/art

_Last verified: 2026-05-06 (Eldoria Art Director, scheduled run-32)_
_Audited against: HEAD `main` (commit `df5dba8`) — auto/art reset to main + run-32 audit-only delta_

This file is auto-generated by the Art Director scheduled task. It tells the
integrator and the next art run what 2D assets are already shipped, so we
don't redo work or burn Firefly credits regenerating icons that already exist.

## Summary

| Category | Count | Notes |
|---|---|---|
| Item icons (root) | **41** | every `Items.gd` ITEMS entry has its PNG (cross-referenced) |
| Affix overlay icons | 13 | 7 prefixes + 6 suffixes |
| Achievement icons | 9 | wired through `Achievements.gd` (run-32 reconciled, +`first_steps`, +`first_forge`) |
| Status-effect icons | 8 | combat HUD overlays |
| Codex flair icons | 2 | faction sigils for the codex |
| NPC + enemy portraits | 24 | 7 Briarwood, 8 expanded-world (incl. `alden_pathfinder`, `owen_vanguard`), 9 enemies (run-32 reconciled from 22) |
| Mood board panels | **11** | palette / era_window / silhouette_check / enemy_silhouettes / lighting_compass / prop_sheet / architecture_palette / magic_glow_reference / ui_chrome / world_map_sketch / biome_palette_reference (run-31 audit reconciled) |
| Town banners | 15 | 7 towns + Whisperwood warning |
| Faction sigils | 12 | per-town sigil PNGs |
| Sky panoramas | 7 | dawn / sunset / night JPGs + 1 HDR |
| UI panel art | 8 | parchment, scroll banner, wood, buttons, divider |
| PBR texture sets | 11 | arch / bark / dirt / grass / rock / snow / stone / terrain / thatch / tidesong / wood (run-32 reconciled from doc-stated 8) |

## Items.gd cross-reference

All **54** icon path references in `eldoria-godot/scripts/Items.gd`
(41 base items + 13 affix overlays) resolve to existing PNGs on `auto/art`.
**0 missing.** Run-25 closed the one outstanding gap (`captain_seal.png`).

## Run-32 audit — what this run found

Run-32 was another **audit-only delta**. The single new commit on
`main` since run-31 (`df5dba8` — animation wiring of
`humanoid_base.glb` to Player + NPC + map attack) is a
character/animation pipeline change that introduces no new
art-owned categories. All five priority art categories remained
at full coverage:

1. `Items.gd` — 41 top-level item keys + 13 affix overlays =
   **54 icon path references**, all resolve to existing PNGs
   under `assets/icons/` and `assets/icons/affix/`. **0 missing.**
2. `Enemy.gd` — kinds: goblin (grunt/brute/warlord), wolf
   (dire), skeleton (warrior), crystal (elemental / guardian),
   bandit (hooded / captain). **9 enemy portraits** present at
   `assets/portraits/{slug}.png`. **0 missing.**
3. `TOWN_MANIFEST.md` — 7 towns (Briarwood, Goldhaven, Ironhold,
   Silverleaf, Stormwatch, Embergrove, Frostpeak) + Whisperwood
   warning, each with banner + sign + sigil + Trade-NPC portrait.
   **0 missing.**
4. Mood-board panels — **11** (palette, era_window,
   silhouette_check, enemy_silhouettes, lighting_compass,
   prop_sheet, architecture_palette, magic_glow_reference,
   ui_chrome, world_map_sketch, biome_palette_reference) per
   run-31's reconciled count.
5. Skies / textures / UI / banners — unchanged from run-31.

Per the Art Director skill rule "ONE category per run" and "if
there is no work, write the audit instead of generating filler",
run-32 produced no new PNGs and burned no Firefly / Canva credits.

### What run-32 reconciled in the summary table

Run-31 flagged two doc-drift items in its trailing narrative
("known pre-run-31 doc drift the next full audit can reconcile")
but did not edit the summary table itself. Run-32 closes that
out:

- **PBR texture sets: 8 → 11.** The `assets/textures/` folder
  contains *eleven* PBR triplet categories on `main` —
  `arch`, `bark`, `dirt`, `grass`, `rock`, `snow`, `stone`,
  `terrain`, `thatch`, `tidesong`, `wood` — not eight. The
  earlier "8/8" claim dates back to the run-25 baseline
  before Environment shipped the snow / tidesong / arch /
  terrain expansions. No new textures shipped this run; the
  delta is doc-only.
- **Achievement icons: 7 → 9.** `assets/icons/achievements/`
  ships *nine* PNGs on `main` — `goblin_bane`, `wolf_tamer`,
  `pack_thinner`, `seal_keeper`, `road_warden`, `realm_warden`,
  `first_steps`, `first_forge`, `trusted_three`. Run-31's
  audit cross-reference noted "appears to have grown to 9
  outside run-30 narrative"; run-32 brings the summary table
  in line.
- **NPC + enemy portraits: 22 → 24.** `assets/portraits/`
  ships *24* PNGs on `main` — 7 Briarwood (`maeve`,
  `smith_edda`, `mara`, `lyra`, `bram`, `roan`, `hala`) +
  8 expanded-world (`ledger_thain`, `gemcutter_durnak`,
  `loremaster_aelin`, `brine_yorra`, `pyra_solenne`,
  `ranger_vorn`, `alden_pathfinder`, `owen_vanguard`) +
  9 enemies. The previous "22" count predated `alden_pathfinder`
  and `owen_vanguard` (Lore/Character-shipped expanded-world
  questgivers).

### Run-32 also documented (no count change)

- **Path-convention "tres-only" item icons (9, all CC0
  procedural).** `assets/icons/{briar_shortbow, roan_woodbow,
  mossbound_buckler, cavestone_buckler, shard_glaive,
  bone_shard, charcoal, iron_ore, leather_strip}.png` ship on
  `main` but are *not* referenced from `Items.gd`'s `ITEMS`
  dict. They are resolved at runtime via the
  `res://assets/icons/{item_id}.png` path convention by
  content-creator-shipped `.tres` ItemResources (see
  `assets/icons/_GENERATED.md` for provenance). They are
  intentional forward-staged art for the Builder/Lore wiring
  pass and **not** orphan art. Future audits should keep
  treating them as covered by the path convention rather
  than gating on `Items.gd` membership.

### Why run-32 didn't ship a new mood-board

Per the run-31 rule restated here verbatim: "if three
consecutive audit-only runs happen with no new upstream work,
the next art-director run can shift to an opportunistic
mood-board panel from the candidate list maintained in the
`mood-boards/README.md` Provenance section." Run-32 is the
**second** consecutive audit-only run after run-31. The next
run (run-33) — if it again finds no new upstream work —
satisfies the three-in-a-row trigger and may pick up an
opportunistic mood-board panel. Run-32 stays in audit posture.

## Run-31 audit — what this run found

Run-31 was an **audit-only delta**. All five priority art categories
remained at full coverage on `main` — no new items in `Items.gd`,
no new enemy kinds in `Enemy.gd`, no new towns in `TOWN_MANIFEST.md`,
no new portraits referenced from upstream Builder/Lore/Character runs
since the run-30 audit. The run-26 mood-board backlog stayed closed
(run-27 architecture, run-28 magic glow, run-29 ui chrome, run-30
world map). Per the Art Director skill rule "ONE category per run"
and "if there is no work, write the audit instead of generating
filler", run-31 produced no new PNGs.

What run-31 did reconcile:

- **Mood-board panel count corrected from 10 → 11.** The
  `mood-boards/biome_palette_reference.png` panel
  (1280×1808, ~360 KB; generator
  `mood-boards/_gen_biome_palette_reference.py`) had been
  shipped between run-29 and run-30 alongside the wider
  expanded-world push (TOWN_MANIFEST + sigils) but was not
  picked up in the run-30 summary table because run-30's
  delta narrative focused exclusively on `world_map_sketch.png`.
  The mood-boards/README.md changelog already documents the
  panel; this is a count-only reconciliation in
  `ART_COVERAGE.md`. No file content changed under
  `mood-boards/` this run.
- **Run-30 baseline preserved.** All run-30 narrative
  sections below are kept verbatim for history. Run-31 adds
  itself above as a separate section per the changelog
  pattern.

### Audit cross-references run-31 ran

1. `Items.gd` — 41 top-level item keys, 41 `icon_path`
   references, 41 PNGs at `assets/icons/{item_id}.png`.
   **0 missing.** No new items since run-30.
2. `Enemy.gd` — kinds: goblin (grunt/brute/warlord),
   wolf (dire), skeleton (warrior), crystal (elemental/
   guardian), bandit (hooded/captain). 9 enemy portraits
   at `assets/portraits/{slug}.png`. **0 missing.**
3. `TOWN_MANIFEST.md` — 7 towns + Whisperwood warning, all
   with banner + sign + sigil + portrait. **0 missing.**
4. `assets/icons/affix/` — 13 overlays (7 prefixes +
   6 suffixes). **0 missing.**
5. `assets/icons/achievements/` — 9 PNGs (was 7 in run-30
   summary; the achievements folder appears to have grown
   to 9 outside run-30 narrative — `goblin_bane`,
   `wolf_tamer`, `pack_thinner`, `seal_keeper`, `road_warden`,
   `realm_warden`, `first_steps`, `first_forge`,
   `trusted_three` — listed via the GitHub contents API).
6. `assets/textures/` — 11 PBR sets across arch / bark /
   dirt / grass / rock / snow / stone / terrain / thatch /
   tidesong / wood. **0 missing** (the run-30 summary said
   "8" but the folder lists 11 categories; this is a known
   pre-run-31 doc drift the next full audit can reconcile —
   this run only fixes the mood-board count).
7. `assets/skies/` — 7 sky panoramas (3 dawn/sunset/night ×
   2 sizes + 1 HDR). **0 missing.**

### Why run-31 didn't ship a new mood-board

Per the skill rule "ONE category per run", run-31 would have
picked up the next backlog mood-board panel. The run-26 backlog
of four panels closed cleanly (run-27→30). Run-30 declared no
new backlog. Sourcing a new mood-board on a clean backlog is
filler work — the Art Director skill explicitly forbids that
("don't redo work or burn Firefly credits regenerating icons
that already exist"). The next run that picks up new upstream
work — a new item, enemy, or town added by Builder/Lore/Character
— should generate the matching asset. If three consecutive
audit-only runs happen with no new upstream work, the next
art-director run can shift to an opportunistic mood-board
panel from the candidate list maintained in the
`mood-boards/README.md` Provenance section.

## Run-30 delta — what shipped this run

Run-30 picked up the last remaining documented gap from the run-29
backlog (`mood-boards/world_map_sketch.png`) and shipped it. Per the
Art Director skill rule "ONE category per run", no other categories
were touched — the existing 41 item icons + 13 affix overlays, 22
portraits, 15 banners, 8 PBR texture sets, 7 sky panoramas, and the
run-27/run-28/run-29 mood-board panels (`architecture_palette.png`,
`magic_glow_reference.png`, `ui_chrome.png`) remain at run-29
coverage.

- **`mood-boards/world_map_sketch.png`** — 1024×1024 RGB, ~98 KB,
  generated by `mood-boards/_gen_world_map_sketch.py` (seed 512,
  sibling to `mood-boards/_generate.py`,
  `_gen_architecture_palette.py`, `_gen_magic_glow_reference.py`,
  and `_gen_ui_chrome.py`). Subject: a Tolkien-style hand-painted
  parchment world map of Eldoria with the four THEME §8 canon
  regions arranged spatially as the player encounters them — a
  Mountain Ring of chevron silhouettes framing all four edges
  (snow-capped along the top, stone-grey down the sides, sunset-
  warmed along the bottom horizon); a Whisperwood oak/pine canopy
  oval in the centre with stamped pine-triangle and oak-blob tree
  silhouettes; a Crystal Caves gothic-pointed stone arch in the NE
  with a soft fey-cyan glow (Gaussian-blurred RGBA composite, kept
  at <30% of frame area per the §1 cool-tone rule); a Briarwood
  Village cottage glyph on the southern lip of the canopy with
  timber-frame walls, thatched roof, lit sunset-gold windows, a
  smoking stone chimney, and a small campfire glyph beside it; and
  a sepia-dotted trail Briarwood → through-Whisperwood → Crystal
  Caves so quest agents can read the canonical journey at a glance.
  Decorations: an ELDORIA title cartouche with bronze inner edge
  and wine-crimson fleuron stops, a hand-drawn 8-point compass
  rose with a wine north-arrow lower-left, a 4-segment scale bar
  ("1 day's march") lower-right, scroll-corner curls at all four
  corners, and a footer caption tracking THEME §3/§8. Schematic-
  not-painterly, matching the existing `palette.png` /
  `prop_sheet.png` / `architecture_palette.png` /
  `magic_glow_reference.png` / `ui_chrome.png` precedent — the
  boards exist to point at decisions, not to inspire by mood (see
  `mood-boards/README.md` Provenance section).

### THEME.md cited (run-30 world_map_sketch.png)
- §1 painterly hand-painted concept-art aesthetic — every
  parchment fill is speckled with `grain()` (±14 brightness
  jitter, 2,400 speckles over the parchment core + 900 darker
  speckles in the scorched-margin band) and every parchment
  frame edge has 60 random brushstroke nicks, so no border
  reads as crisp vector. The Crystal Caves cyan glow is
  Gaussian-blurred (radius 6) so the §1 "no crisp vector"
  beat applies to the magic accent, not just the parchment.
- §1 cool tones reserved for night, mist, magic — the fey-cyan
  glow is the only cool fill on the panel; it stays inside its
  cave-mouth radius so the cool-tone band is well under §1's
  <30%-of-frame budget. The rest of the page is parchment +
  wood + sunset / wine / moss / stone — warm-band canon.
- §3 palette — `PARCHMENT` / `PARCHMENT_LT` / `PARCHMENT_DK`,
  `DARK_WOOD` / `MID_WOOD` / `LIGHT_WOOD`, `SUNSET_GOLD`,
  `BURNT_ORANGE`, `WINE_CRIMSON`, `HAMMERED_BRONZE`,
  `STONE_DK` / `STONE_GREYBLUE`, `MOSS_DK` / `MOSS_LT`,
  `INK_BLACK` / `INK_FADED` / `SEPIA`, `FEY_CYAN`, and
  `THATCH_LT` imported verbatim from `_generate.py`. No new
  hues introduced — the only "magic" colour used is `FEY_CYAN`,
  and only for the Crystal Caves glow per the §3 magic-accent
  rule. A `PARCHMENT_BRN` derivative is used for the scorched-
  edge band only and is explicitly within the §3 parchment
  band (no new canonical hue introduced).
- §5 typography & UI — title cartouche has a 2-px wood frame
  + 1-px hammered-bronze inner edge + wine-crimson fleuron
  stops (the §5 "wood-and-iron frames" beat); region labels
  use a serif body font (DejaVuSerif, the Cinzel/EB-Garamond
  proxy on this system) on parchment-light backings; the
  ELDORIA title is rendered in serif-bold (the blackletter
  beat is reserved for in-game UI per §5 "used SPARINGLY for
  impact only"). No glassmorphism, no Material/Fluent/iOS-flat,
  no sharp-cornered rectangle without a frame.
- §8 architecture & environment — Briarwood Village glyph
  uses timber-frame walls + thatch roof + lit candle window +
  smoking chimney (matches the run-27 `architecture_palette.png`
  Briarwood-cottage cell); Whisperwood is dense oak/pine
  silhouettes with mossy variance (matches §8 "dense oak/pine
  forest" wording); Crystal Caves entrance is a gothic-pointed
  stone arch with a soft cyan glow (matches §8 "cool blue glow,
  crystal formations on walls, ancient runes carved into stone");
  Mountain Ring is impassable peak silhouettes framing every
  edge (matches §8 "distant impassable peaks with snow caps,
  forming the horizon"). Spatial arrangement places Briarwood
  on the southern forest edge per §8 wording, Whisperwood
  central, Crystal Caves NE, Mountain Ring all four edges.
- §7 child-safe — no skulls / blood / wounds / weapons on the
  map. The campfire glyph next to Briarwood reads as warmth +
  hearth, not danger. The Crystal Caves arch is empty (no
  monster silhouette) so the entrance reads as mystery, not
  threat — boss-room fear belongs in the Crystal Caves
  interior assets, not the world map.
- §10 hard rules — pinned `random.seed(512)` for byte-stable
  re-renders (sibling seeds: 206 / 307 / 411 from earlier
  `_gen_*` runs); no Firefly / Canva / external assets — Pillow
  procedural only; every committed colour is in the §3 canon
  band (or a `_generate.py` derivative explicitly within
  band).

### Mood board panel (run-30)
- `mood-boards/palette.png` — sampled `MOSS_DK` and `MOSS_LT`
  bands so the Whisperwood canopy reads in the same §3 forest
  family as `briarwood_village.png` and the run-27
  `architecture_palette.png` cottage cell; sampled
  `STONE_DK` / `STONE_GREYBLUE` so the Mountain Ring reads in
  the same warm/cool stone family as `stone_diff.jpg` and the
  Smithy / watchtower cells from run-27; sampled `FEY_CYAN`
  for the Crystal Caves glow only — the same hex used by
  `_gen_magic_glow_reference.py` (run-28) for the cyan-row
  swatches, so VFX/Lighting agents can pick the
  Caves-mouth band by name from a calibrated source.
- `mood-boards/architecture_palette.png` — the Briarwood-cottage
  cell from run-27 is the source-of-truth glyph that the
  world-map's Briarwood marker shrinks down to (timber-frame +
  thatch + lit window + smoking chimney). Same shape language
  inside two scales — the architecture_palette is the close-up,
  the world_map_sketch is the wide.

### Backlog after run-30

The four-panel mood-board backlog from run-26 is now closed.
Future art runs should pick up new categories that materialise
from upstream Builder / Lore / Character runs (e.g. new towns
added to `TOWN_MANIFEST.md`, new enemy IDs added to `Enemy.gd`
drop tables, new items in `Items.gd` introduced after this
audit). The next run should re-audit against this file as the
baseline and only generate the *delta*.

## Run-29 delta — what shipped this run

Run-29 picked up the next-oldest documented gap from the run-28
backlog (`mood-boards/ui_chrome.png`) and shipped it. Per the Art
Director skill rule "ONE category per run", no other categories
were touched — the existing 41 item icons + 13 affix overlays,
22 portraits, 15 banners, 8 PBR texture sets, 7 sky panoramas,
and the run-27/run-28 mood-board panels (`architecture_palette.png`,
`magic_glow_reference.png`) remain at run-28 coverage.

- **`mood-boards/ui_chrome.png`** — 1024×1024 RGB, ~119 KB,
  generated by `mood-boards/_gen_ui_chrome.py` (seed 411,
  sibling to `mood-boards/_generate.py`,
  `_gen_architecture_palette.py`, and
  `_gen_magic_glow_reference.py`). Subject: a 2×2 schematic
  cell grid showing the assembled UI chrome look — Cell 1
  ANATOMY OF A PANEL (parchment + wood-and-iron frame +
  ornate divider + corner iron studs + brushstroke edge,
  with arrow call-outs labelling each layer); Cell 2
  ORNATE DIVIDERS (small for between-fields, medium for
  section-break with a wine-crimson lozenge, large for
  chapter/quest break with fleuron + flanking arabesque
  dots); Cell 3 BUTTON STATES (the same parchment+frame
  primitive in idle/hover/pressed/disabled, with `modulate`
  tinting the parchment darker on press and lighter on
  hover, and a second row showing a "Refuse" secondary CTA
  themed by a translucent wine-veil overlay rather than a
  new hue); Cell 4 COMPOSED QUEST PANEL (a fully-assembled
  quest dialog: blackletter-spec'd title + large fleuron
  divider + serif body paragraphs + objective bullets +
  Accept/Refuse CTA pair). The board exists so UI agents
  can cite ONE panel and see how the primitives compose,
  instead of reading three isolated atlas pieces and
  guessing how they assemble. Schematic-not-painterly,
  matching the existing `palette.png` / `prop_sheet.png` /
  `architecture_palette.png` / `magic_glow_reference.png`
  precedent — the boards exist to point at decisions, not
  to inspire by mood (see `mood-boards/README.md` Provenance
  section).

### THEME.md cited (run-29 ui_chrome.png)
- §1 painterly hand-painted feel — every parchment fill is
  speckled with `grain()` (±14 brightness jitter, 110–240
  speckles per surface) and every frame edge has 12 random
  brushstroke nicks, so no border reads as crisp vector.
- §3 palette — `PARCHMENT` (`#D9C99B`), `DARK_WOOD`,
  `HAMMERED_BRONZE` (`#B0742A`), `WINE_CRIMSON` (`#8C2020`),
  `INK_BLACK` (`#0E0A0E`), and `STONE_GREYBLUE` (`#7B8693`,
  disabled-button frame) imported verbatim from `_generate.py`.
  No new hues introduced — the secondary "Refuse" CTA themes
  via a 36-alpha wine veil overlay rather than a new colour
  band.
- §5 typography & UI — every panel has a 8–12 px corner
  radius (`rounded_rectangle` with a hand-fallback path for
  older Pillow); every panel has a wood-and-iron frame with
  four corner iron studs (the §5 "wood-and-iron frames"
  beat); the bronze inner edge stands in for the §5
  "slight irregularity, brushstroke edges" requirement.
  Banned-styles compliance: no glassmorphism, no
  Material/Fluent/iOS-flat panel, no sharp-cornered
  rectangle anywhere.
- §5 typography — body text uses `DejaVuSerif` (the
  Cinzel/EB-Garamond proxy on this system); the
  blackletter-spec'd quest title is rendered in serif-bold
  with a wine-crimson fill + parchment-dark drop-shadow
  because UnifrakturMaguntia isn't installed on the
  generator host (the schematic board is for layout, not
  specimen rendering — painterly UI specimen art belongs
  under `concept/` per `ARTIST_AGENT.md`, called out in the
  panel's footer caption).

## Run-28 delta — what shipped this run

Run-28 picked up the next-oldest documented gap from the run-26 backlog
(`mood-boards/magic_glow_reference.png`) and shipped it. Per the Art
Director skill rule "ONE category per run", no other categories were
touched — the existing 41 item icons + 13 affix overlays, 22 portraits
(7 Briarwood + 6 expanded + 9 enemies inc. `bandit_captain`), 15 banners,
8 PBR texture sets, 7 sky panoramas, and the run-27 mood-board panel
(`architecture_palette.png`) remain at run-27 coverage.

- **`mood-boards/magic_glow_reference.png`** — 1024×1024 RGB, ~120 KB,
  generated by `mood-boards/_gen_magic_glow_reference.py` (seed 307,
  sibling to `mood-boards/_generate.py` and `_gen_architecture_palette.py`).
  Subject: a 3×3 schematic mood-board grid — three rows for the three
  THEME §3 canon magic hues (fey cyan `#65DFE5` / warlock purple `#7C3FB0`
  / frost silver `#C8E0E5`), three columns for calibrated intensity
  bands (LOW = ambient ward / herb glow at intensity ≈0.35, MED =
  spellcast / unique-item halo at ≈0.6, HIGH = boss aura / Crystal
  Caves veins at ≈0.95). Each swatch paints a dusk gradient (deep
  indigo → warlock-shaded blue-purple → wine-warm horizon afterglow →
  moss-shadow ground ridge), drops a stone rune-slab emitter at the
  glow's anchor, layers five additive-radial ellipse bands at the
  target hue + intensity, and Gaussian-blurs the layer for a §5
  painterly soft rim before alpha-compositing onto the dusk base. A
  hex-code chip in the upper-left of each swatch + a three-bar
  intensity meter beneath the swatch let VFX/Lighting agents pick a
  hue+intensity combination by name instead of guessing pixels.
  Schematic-not-painterly, matching the existing `palette.png` /
  `prop_sheet.png` / `architecture_palette.png` precedent — the boards
  exist to point at decisions, not to inspire by mood (see
  `mood-boards/README.md` Provenance section).

### THEME.md cited (run-28 magic_glow_reference.png)
- §1 cool tones reserved for night, mist, magic — every glow stays
  at <30% of frame area inside its swatch (the dusk + ground band
  takes the rest), and the page-level header + footer + cell frames
  use warm wine + parchment + dark-wood so the cool fills aren't
  globally dominant.
- §3 magic palette — `FEY_CYAN`, `WARLOCK_PURPLE`, and `FROST_SILVER`
  imported verbatim from `_generate.py`'s canon constants. No new
  hues introduced. Hex codes shown in the swatch chips so the
  source-of-truth values aren't ambiguous.
- §5 painterly, hand-painted look, not crisp vector — additive
  radial ellipse bands + Pillow `GaussianBlur` on the alpha layer
  before alpha-compositing, so every halo has a soft rim and no
  hard outline. Rune-slab emitter drawn with §3 `STONE_DK` + a
  hue-tinted chevron, not a crisp vector glyph.
- §1 painterly speckle precedent — `grain()` function reused
  byte-identically from `_generate.py` for the dusk afterglow band
  and the moss-shadow ground stripe.
- §10 hard rules — every committed colour is in the §3 canon band;
  pinned `random.seed(307)` for byte-stable re-renders; no Firefly /
  Canva / external assets — Pillow procedural only.

### Mood board panel (run-28)
- `mood-boards/palette.png` — sampled `FEY_CYAN`, `WARLOCK_PURPLE`,
  and `FROST_SILVER` directly from the §3 magic-band swatches. The
  three rows of `magic_glow_reference.png` are the dusk-tested glow
  derivatives of those three palette chips — same hex, calibrated
  alpha layering, sanity-checked against a cool dusk so the boss-aura
  case (HIGH intensity) doesn't bleach the warm-band canon when the
  player triggers it.

## Run-27 delta — what shipped this run

Run-27 picked up the oldest documented gap from the run-26 backlog
(`mood-boards/architecture_palette.png`) and shipped it. Per the
Art Director skill rule "ONE category per run", no other categories
were touched — the existing 41 item icons, 22 portraits, 15 banners,
8 PBR texture sets, and 7 sky panoramas remain at run-26 coverage.

- **`mood-boards/architecture_palette.png`** — 1024×1024 RGB,
  ~67 KB, generated by `mood-boards/_gen_architecture_palette.py`
  (seed 206, sibling to `mood-boards/_generate.py`). Subject: a 6-cell
  schematic mood board of Eldoria's structure archetypes —
  Briarwood cottage (timber-frame + thatch + lit candle window +
  smoking chimney), the Inn (two-story timber-frame + double doors +
  hanging mug-glyph sign + three lit upstairs windows), Smithy Edda
  (stone-built fire-safe walls + glowing forge + anvil silhouette +
  big chimney with smoke plumes + ember spark), Whisperwood goblin
  tent (hide drape + crude support poles + red rag-banner +
  bone-on-stake silhouette + campfire embers), Crystal Caves entrance
  (gothic-pointed stone arch + cyan inner glow + crystal shards on
  rim + carved-rune keystone + mossy-stone backdrop), and Mountain
  Ring watchtower (crenellated parapet + wine banner + arrow-slit +
  distant peaks). Schematic-not-painterly, matching the existing
  `palette.png` and `prop_sheet.png` precedent — the boards exist to
  point at decisions, not to inspire by mood (see `mood-boards/README.md`
  Provenance section).

### THEME.md cited (run-27 architecture_palette.png)
- §1 painterly schematic — same Pillow speckle / soft-edge primitive
  silhouettes as `prop_sheet.png`. Pinned random seed for byte-stable
  re-renders.
- §3 palette — every fill is from the canon constants imported
  verbatim from `_generate.py` (PARCHMENT, DARK_WOOD, MID_WOOD,
  THATCH_LT/DK, STONE_LT/DK, FEY_CYAN, WINE_CRIMSON, SUNSET_GOLD,
  BURNT_ORANGE, FLAME_HOT). No new canonical hues introduced.
- §8 architecture — timber-frame + daub + thatch for Briarwood
  cottage and Inn (matches the existing `building_door`,
  `whisperwood_floor`, and `briarwood_path` texture sets); stone
  for the Smithy and watchtower (matches `stone_diff.jpg`,
  `curtain_wall_stone`); hide-and-stick for the goblin tent; and
  arched stone for the Crystal Caves entrance — all in §8 era band.
- §7 child-safe — goblin tent skull is a schematic two-circle-eye
  silhouette, no gore, no blood, no missing features. Crossed-daggers
  and other heraldic motifs intentionally absent.
- §1 "cool tones reserved for night, mist, magic" — only the Crystal
  Caves cell uses the cyan/cool family (FEY_CYAN); the other five
  cells are warm (sunset-orange forge, gold-lit windows,
  thatch-warm roofs, crimson banners). The bottom-row caption
  states the cool palette stays at <30% of frame.

### Mood board panel (run-27)
- `mood-boards/palette.png` — sampled `THATCH_LT`/`THATCH_DK`,
  `STONE_LT`/`STONE_DK`, and `MID_WOOD` bands so the cottage / inn /
  smithy walls read in the same warm-timber-and-stone family as
  `briarwood_village.png`, the existing `building_door` texture
  set, and the run-23 `prop_sheet.png` lantern + signpost cells.
  `FEY_CYAN` borrowed from §3 magic-accent for the Crystal Caves
  cell only — used at <30% per the §1 cool-tone rule.
- `mood-boards/prop_sheet.png` — the cottage cell's chimney + smoke
  treatment matches the lantern flame-glow rendering from
  `_draw_lantern`; the goblin tent's campfire embers reuse the
  forge ring-pattern from `_draw_well`'s wood pivot pattern.

## Run-26 delta — what shipped that run (preserved for history)

The Character agent's recent runs introduced `bandit_captain` as a
dedicated mini-boss `enemy_kind` in `Enemy.gd` (KIND_MODELS +
KIND_TINT_OVERRIDE), with its own scale (1.40×) and purple-leather
tint distinguishing it from the regular `bandit` kind. Run-25 closed
the icon-side gap by shipping `captain_seal.png` (the trophy material).
The portrait-side gap — a face for the codex / boss-intro splash /
loot-toast header — was still open: the 8 enemy portraits in
`gen_enemy_portraits.py` covered `bandit` via `bandit_hooded.png`,
but `bandit_captain` had no silhouette-distinct face yet. At codex
thumbnail size, a regular hooded scarfed bandit and a captain should
not read identical.

Run-26 (this commit) ships the painterly portrait:

- **`bandit_captain.png`** — 256×256 RGBA, ~32 KB, generated by
  `scripts/art/gen_bandit_captain_portrait.py` (seed 7509, additive
  to the 7501..7508 range used by `gen_enemy_portraits.py`). Subject:
  a painterly bust of a bandit captain — wider tricorne hat with
  feather plume on the right brim, weathered visible (unscarfed)
  face with a single thin cheek scar, narrowed iron-grey eyes,
  broader shoulder line with a purple-leather pauldron carrying
  four brass rivets, and a brass-disc crossed-daggers chest emblem
  matching the run-25 `captain_seal.png` motif so the codex face
  + the trophy item share a visual signature. Background gradient
  pushes from purple-wine top to deep ink-purple bottom — silhouette-
  distinct against `bandit_hooded.png`'s charcoal-leather backdrop
  in side-by-side codex panels.

### Run-25 delta — what shipped that run (preserved for history)

The Builder agent's run-24 added `captain_seal` — a rare bandit-captain
trophy material — to `Items.gd` ITEMS and to the `bandit_captain` drop
table at weight 16. The icon_path was wired through (`res://assets/icons/
captain_seal.png`) but the PNG wasn't yet shipped, so the Inventory grid
fell back to the 🕯 emoji per the run-24 fail-soft contract.

Run-25 (the previous commit) shipped the painterly PNG:

- **`captain_seal.png`** — 128×128 RGBA, 12.5 KB, generated by
  `scripts/art/gen_captain_seal_icon.py`. Subject: a circular iron-cast
  hand-stamp disc with patinated warm-brown surface (Items.gd anchor
  `Color(0.55,0.45,0.30)`), a small top loop with a leather thong fragment
  threaded through, embossed crossed-daggers bandit emblem on the face,
  weathered pitting and moss flecks. Sunset highlight crescent on the
  upper-left rim per the THEME §3 sunset-accent contract used by the rest
  of the loot pipeline. Painterly parchment backdrop matches the run-23
  / run-24 inventory-grid look (wolf_fang, wolf_pelt, wolf_heart,
  wooden_shield).

### THEME.md cited (run-26 bandit_captain.png)
- §1 painterly, hand-painted concept-art aesthetic — same `_draw_painter_blob`
  soft-edge ellipse stamping + 0.30·SUPER Gaussian blur + LANCZOS
  downsample as `gen_enemy_portraits.py`. No crisp vector edges.
- §3 palette — leather dark, wine, ink, brass, parchment, deeper
  purple-leather (LEATHER_PURP) on the pauldron only. Arcane purple
  used sparingly per the §3 magic-accent rule. No neon, no fluorescent.
- §4 silhouette-distinct from `bandit_hooded.png` — tricorne brim
  instead of pointed hood; visible (unscarfed) face; broader shoulder
  with purple-leather pauldron + brass rivets; crossed-daggers chest
  emblem; feather plume on the right brim. Captain reads as "officer"
  not "rogue" at codex-thumbnail size.
- §5 hand-painted, not crisp vector — same painterly speckle frame
  + soft rounded-rect alpha mask as the existing 8 enemy portraits.
- §7 child-safe — single thin cheek scar (no wounds, no blood, no
  missing eye); crossed-daggers motif (period weaponry, §2 allowed)
  not skulls or gore.
- §10 Hard Rule 9 — older / weathered / hand-made: weathered hat
  brim, frayed cloak edge, eye-bag stubble dab pattern.

### Mood board panel (run-26)
- `mood-boards/palette.png` — sampled `LEATHER_DK`, `WINE`, and `BRASS`
  bands so the captain reads in the same warm-leather family as
  `bandit_hooded.png`, the run-25 `captain_seal.png`, and the
  Whisperwood-bandit-camp banner work; `LEATHER_PURP` introduced as
  the captain-only insignia colour matching `Enemy.gd`'s
  KIND_TINT_OVERRIDE comment for `bandit_captain`.

### THEME.md cited (run-25 captain_seal.png — preserved for history)
- §1 painterly, hand-painted concept-art aesthetic — soft Gaussian rims,
  brushy speckle, gentle drop shadow. No crisp vector edges.
- §2 era — late medieval iron-cast hand-stamp seal on leather thong,
  period-correct (1300–1500 European feel). Crossed daggers are §2
  allowed weaponry.
- §3 palette — desaturated brass/bronze body, parchment ground, ink
  rim, sunset highlight rim. No neon, no fluorescent, no pure white.
- §5 hand-painted look, not crisp vector — final 0.30·SUPER Gaussian
  blur + LANCZOS downsample blends edges; soft circular alpha mask
  prevents any boxy disc edge.
- §7 child-safe — bandit motif uses crossed daggers, not skulls or gore.

### Mood board panel (run-25)
- `mood-boards/palette.png` — sampled `IRON_PATINA` from THEME §3 brass
  band so the disc reads in the same warm-brown family as the existing
  `warlord_horn`, `wooden_shield`, and `goblin_ear` icons in the
  inventory grid.

## NPC portrait roster (Briarwood, hard-required by §4 of THEME.md)

- Elder Maeve → `portraits/maeve.png`
- Smith Edda → `portraits/smith_edda.png`
- Mara the Merchant → `portraits/mara.png`
- Herbalist Lyra → `portraits/lyra.png`
- Innkeeper Bram → `portraits/bram.png`
- Stablemaster Roan → `portraits/roan.png`
- Trainer Hala → `portraits/hala.png`

All seven shipped. ✅

## Texture coverage

- **bark** (3 files): bark_diff.jpg, bark_norm.jpg, bark_rough.jpg
- **dirt** (3 files): dirt_diff.jpg, dirt_norm.jpg, dirt_rough.jpg
- **grass** (3 files): grass_diff.jpg, grass_norm.jpg, grass_rough.jpg
- **rock** (3 files): rock_diff.jpg, rock_norm.jpg, rock_rough.jpg
- **snow** (3 files): snow_diff.jpg, snow_norm.jpg, snow_rough.jpg
- **stone** (3 files): stone_diff.jpg, stone_norm.jpg, stone_rough.jpg
- **thatch** (6 files): roof_diff.jpg, roof_norm.jpg, roof_rough.jpg, shingle_diff.jpg, shingle_norm.jpg, shingle_rough.jpg
- **wood** (3 files): wood_diff.jpg, wood_norm.jpg, wood_rough.jpg

## What this run did (run-28)

Run-28 was a **delta-only** mood-board pass. It picked up
`mood-boards/magic_glow_reference.png` from the run-27 backlog (oldest-
first ordering after run-27 closed `architecture_palette.png`) and
shipped it via a procedural Pillow generator
(`mood-boards/_gen_magic_glow_reference.py`, seed 307, byte-stable on
re-run). All other priority categories remain at run-27 coverage —
41 item icons + 13 affix overlays, 22 portraits, 15 banners, 8 PBR
texture sets, 7 sky panoramas, and 8 mood-board panels (was 7 → now
8 with `magic_glow_reference.png`). Backlog now has three remaining
panels: `ui_chrome.png`, `world_map_sketch.png`, and any new gaps
that materialize from upstream Builder/Lore/Character runs between
now and the next Art run.

_Update (run-29): `ui_chrome.png` shipped this run — see Run-29
delta section above. Backlog after run-29 has one remaining mood-
board panel: `world_map_sketch.png`._

## What this run did (run-27)

Run-27 was a **delta-only** mood-board pass. It picked up `mood-boards/architecture_palette.png` from the run-26
backlog (oldest-first per skill rule), wrote a sibling generator
(`_gen_architecture_palette.py`) following the `_generate.py`
pinned-seed pattern, and shipped the panel + the README/coverage
updates in a single commit. No Firefly credits, no Canva calls,
no asset/icon/portrait/banner/texture/sky changes — those
categories remain at run-26 coverage and are listed in the same
table at the top of this file.

## What earlier runs did (preserved for history)

Earlier runs were **delta-only** generation passes. Per the Art Director skill
rule "ONE category per run", run-26 picked up the single new
`bandit_captain` enemy_kind that materialized in `Enemy.gd` between
run-25 and now and shipped its bestiary portrait `bandit_captain.png`.
Run-25's earlier `captain_seal.png` (the trophy item) and run-26's
`bandit_captain.png` (the enemy face) now share the crossed-daggers
visual signature — the codex panel and the loot toast read as one
adversary across both surfaces. All other priority categories remain
at run-25 coverage:

1. Item icons — **41/41** + 13 affix overlays (run-25 closed the gap)
2. NPC portraits — complete (7/7 town + 6 expanded)
3. Enemy portraits — **9/9** (run-26 added bandit_captain to the
   prior 8: goblin_grunt, goblin_brute, goblin_warlord, dire_wolf,
   skeleton_warrior, crystal_elemental, crystal_guardian, bandit_hooded)
4. Faction banners — complete (7 towns + 6 signs + Whisperwood warning + sigils)
5. PBR textures — complete (8/8 categories with diff+norm+rough)
6. Sky HDRI — complete (sunset 2k + dawn + night + 1 HDR)

Future art runs should pick up new categories that materialize from
Builder/Lore/Character runs (e.g. new towns added to `TOWN_MANIFEST.md`,
new enemy IDs added to `Enemy.gd` drop tables, new items in `Items.gd`
introduced after this audit). The next run should re-audit against this
file as the baseline and only generate the *delta*.

### Backlog — observed during run-26 but not picked up

These are documented gaps the next art run can claim (oldest-first):

- ~~`mood-boards/architecture_palette.png`~~ — **shipped run-27.**
  Briarwood cottage + Inn + Smithy + Goblin tent + Crystal Caves arch +
  Mountain watchtower. See `mood-boards/_gen_architecture_palette.py`.
- ~~`mood-boards/magic_glow_reference.png`~~ — **shipped run-28.**
  3×3 grid (fey cyan / warlock purple / frost silver) × (LOW / MED /
  HIGH) against a dusk backdrop with horizon afterglow + tree-line
  ridge. See `mood-boards/_gen_magic_glow_reference.py`.
- ~~`mood-boards/ui_chrome.png`~~ — **shipped run-29.** 2×2 grid:
  anatomy of a panel + ornate dividers (small/medium/large) + button
  states (idle/hover/pressed/disabled × primary + secondary CTAs) +
  composed quest panel. See `mood-boards/_gen_ui_chrome.py`.
- ~~`mood-boards/world_map_sketch.png`~~ — **shipped run-30.**
  Tolkien-style hand-painted parchment map: Mountain Ring framing
  all four edges, Whisperwood oak/pine canopy oval centre,
  Crystal Caves arch + cyan glow NE, Briarwood Village glyph S,
  sepia-dotted trail Briarwood → Whisperwood → Crystal Caves.
  Title cartouche + compass rose + scale bar + scroll-corner
  curls. See `mood-boards/_gen_world_map_sketch.py` (seed 512).

All four are procedural-Pillow panels by precedent (see
`mood-boards/_generate.py`); no Firefly credits required.
The four-panel run-26 backlog is now closed (run-27 architecture,
run-28 magic glow, run-29 ui chrome, run-30 world map).

## License audit reminder

All committed PNGs to date are flagged in their per-folder `ATTRIBUTION.md`
or `ENEMIES_ATTRIBUTION.md` siblings as Firefly-generated, Canva-generated,
PolyHaven CC0, or Adobe Stock CC0. The new `bandit_captain.png` (run-26)
and `captain_seal.png` (run-25) are CC0 — both generated procedurally
with Pillow by committed scripts (no third-party images, no fonts,
no trademarks). Re-running `gen_bandit_captain_portrait.py` with seed
7509, or `gen_captain_seal_icon.py` with seed 20260506, reproduces
byte-identical output.
